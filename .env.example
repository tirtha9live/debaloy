# Local Wrangler / Astro secrets. Copy to .dev.vars (gitignored).
# Production values are Worker secrets on the "debaloy" Worker only.
RESIDENT_PASSWORD=
ADMIN_PASSWORD=

# Optional. Matches wrangler.toml [vars] DEBUG — "true" shows maintenance messaging.
# DEBUG=false
